# n8n Configuration
# For n8n Cloud: N8N_API_URL=https://your-instance.app.n8n.cloud/api/v1
# For local: N8N_API_URL=http://localhost:5678/api/v1
N8N_API_URL=http://localhost:5678/api/v1
N8N_API_KEY=your_n8n_api_key

# Local Docker setup (ignore if using n8n Cloud)
N8N_HOST=localhost
N8N_PROTOCOL=http
WEBHOOK_URL=http://localhost:5678/

# n8n Authentication
N8N_BASIC_AUTH_USER=admin
N8N_BASIC_AUTH_PASSWORD=changeme

# Database
POSTGRES_DB=n8n
POSTGRES_USER=n8n
POSTGRES_PASSWORD=changeme

# Redis
REDIS_PASSWORD=changeme

# Timezone
TIMEZONE=America/New_York

# Monitoring
GRAFANA_USER=admin
GRAFANA_PASSWORD=changeme

# API Configuration
CLICKUP_API_URL=http://clickup-mock:3001
REPLY_API_URL=http://replyio-mock:3002

# API Keys
CLICKUP_API_KEY=your_clickup_api_key
REPLY_API_KEY=your_reply_api_key

# ClickUp Workspace IDs (get these from ./scripts/get-clickup-ids.sh)
CLICKUP_TEAM_ID=
CLICKUP_SPACE_ID=
CLICKUP_FOLDER_ID=
CLICKUP_LIST_ID=

# Debug
DEBUG=false
LOG_LEVEL=info

# =============================================================================
# GitHub Actions Integration (for auto-deploying client workflows)
# =============================================================================
# Personal Access Token with repo and workflow permissions
GITHUB_PAT=ghp_your_personal_access_token
# Repository owner (your GitHub username or org)
GITHUB_REPO_OWNER=your-username
# Repository name
GITHUB_REPO_NAME=qtalo-n8n

# =============================================================================
# Production Notes
# =============================================================================
# For production deployment:
# 1. Copy .env.production.example to .env.production
# 2. Change CLICKUP_API_URL=https://api.clickup.com/api/v2
# 3. Change REPLY_API_URL=https://api.reply.io/v1
# 4. Update N8N_PROTOCOL=https and N8N_HOST=your-domain.com
# 5. Use managed database (RDS, Cloud SQL, etc.)
# 6. Set strong passwords for all services
# See PRODUCTION.md for complete guide
